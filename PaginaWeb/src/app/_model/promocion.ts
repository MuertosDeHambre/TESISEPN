export class Promocion{
    id?: string;
    fotosPromocion: any;
    userUID?: string; // id del usuario logueado (el que actualiza el perfil) 
    fileRef?: string;
    estado?: string;
    //promocion?: any;
}